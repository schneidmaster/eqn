dependencies:
  post:
    - bundle exec appraisal install
    - bin/compile

test:
  override:
    - bundle exec appraisal rspec
    - bundle exec rubocop
  post:
    - bundle exec codeclimate-test-reporter
